### New Features
* Add Ctrl-drag region movement and corner handles for resizing selections.
* Support region captures spanning multiple outputs in the screenshot backend.

### Fixes
* Keep moved selections inside the capture bounds.
* Prevent floating editor tooltips from overlapping the toolbar.
