### New Features
* Add an IME-compatible inline text editor with multiline input and `Ctrl+Enter` to commit.

### Fixes
* Preserve annotation strokes when switching between fullscreen and windowed editor modes.
* Remove the outdated warning claiming that inline mode does not support IME.
